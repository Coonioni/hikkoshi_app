<%= render 'shared/flash_messages' %>

<h1>投稿一覧</h1>
<% @posts.each do |post| %>
  <%= post.id %>
  <%= post.post_content %>
  <%= post.address %>  



<% if user_signed_in? %>
  <%= link_to "投稿", "/posts/new" %>
  <%= link_to "編集", edit_post_path(post.id), method: :get %>
<% end %>

<% end %>